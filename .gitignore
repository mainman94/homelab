*.terraform
*.tfstate
*.tfstate.backup
*.vscode
.terraform.lock.hcl
.terraform/
.ansible
.vscode
.idea
kubeconfig
talosconfig
.at
.claude

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
